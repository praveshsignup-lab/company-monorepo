# Scraped by the platform Prometheus (selectors are cluster-wide).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: service-a
  labels:
    # Must match kube-prometheus-stack's release label so it picks us up.
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: service-a
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      relabelings:
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
        # Stamp job=service-a so SLO query labels are predictable.
        - targetLabel: job
          replacement: service-a
