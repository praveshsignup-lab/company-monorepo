# Per-team Slack webhook URLs. STUB — replace with sealed-secrets in real
# deployment; never commit real webhook URLs.
# Keys are referenced from platform-routing.yaml (AlertmanagerConfig).
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-team-webhooks
  namespace: sidekick-monitoring
type: Opaque
stringData:
  platform-sre-slack: "https://hooks.slack.example/REPLACE_ME_platform_sre"
  team-a-slack:       "https://hooks.slack.example/REPLACE_ME_team_a"
  team-b-slack:       "https://hooks.slack.example/REPLACE_ME_team_b"
  team-c-slack:       "https://hooks.slack.example/REPLACE_ME_team_c"
